{
  "name": "occupied-backend",
  "version": "0.1.0",
  "description": "Backend server for occupied!",
  "main": "app.js",
  "scripts": {
    "start": "node app.js",
    "dev": "npm i && node app.js &>> server.log && cd client/ && npm i && npm start",
    "build": "cd client/ && npm install --production && npm run build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "MIT",
  "dependencies": {
    "axios": "^0.18.1",
    "bcrypt": "^3.0.5",
    "bootstrap": "^4.1.1",
    "date-fns": "^1.30.1",
    "express": "^4.16.4",
    "jsonwebtoken": "^8.5.1",
    "lodash": "^4.17.11",
    "pg": "^7.10.0",
    "sequelize": "^4.43.0"
  },
  "devDependencies": {
    "dotenv": "^8.0.0"
  }
}
